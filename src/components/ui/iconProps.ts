import type { LucideProps } from 'lucide-react'

/** Icônes UI : taille et trait uniformes (style pro, lisible). */
export const iconSm: Pick<LucideProps, 'size' | 'strokeWidth'> = {
  size: 18,
  strokeWidth: 1.65,
}

export const iconMd: Pick<LucideProps, 'size' | 'strokeWidth'> = {
  size: 20,
  strokeWidth: 1.65,
}

export const iconLg: Pick<LucideProps, 'size' | 'strokeWidth'> = {
  size: 22,
  strokeWidth: 1.55,
}
